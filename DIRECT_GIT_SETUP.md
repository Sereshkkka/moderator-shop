# Direct Git workflow

This project root is now safe to push directly to GitHub.

Ignored local files:

- `.env` and other local env files
- `node_modules/`
- `_snapshots/`
- `forgit/`
- local logs and editor metadata

Use `auto-commit.bat` from the project root to commit and push changes.
